# if [[ $(printenv | grep WARP) ]]; then
#   [[ ! -f ~/.p10k-warp.zsh ]] || source ~/.p10k-warp.zsh
# else
#   [[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
# fi

[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
